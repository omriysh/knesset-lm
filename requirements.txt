# HTTP / caching
requests
requests-cache

# Web framework
fastapi
uvicorn[standard]
pydantic
jinja2
aiofiles

# Document parsing (Knesset protocols)
beautifulsoup4
pdfplumber
pymupdf
python-docx
pywin32          # Windows only — legacy .doc extraction via Word COM

# Name matching
rapidfuzz

# Optional: DictaBERT lemmatizer (config.USE_DICTABERT_LEMMA)
transformers>=4.57.0
torch

# Utilities
tqdm

# Optional: Google Gemini backend
google-genai
