/**
 * browser.js — Protocol Browser panel
 *
 * Inline deep-dive panel that appears in the chat column when the user
 * opens a meeting from the Meeting List panel.
 *
 * Phase 2E — stub.  openProtocolBrowser() is called by app.js meeting-
 * select handling; actual transcript/summary rendering is TODO.
 */

// eslint-disable-next-line no-unused-vars
function openProtocolBrowser(sessionId, meetingId, meetingTitle) {
  // TODO: Phase 2E — fetch transcript + summary, render inline panel
  console.log('[browser] openProtocolBrowser', sessionId, meetingId, meetingTitle);
}
